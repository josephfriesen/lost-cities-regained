# import Player, Game, Round, Card from "./generated/prisma.graphql"

type Query {
  getAllCards: [Card!]!
  constructRandomizedDeckInstances: [CardInstance!]!
}

type Mutation {
  newPlayer(name: String, email: String): Player
  newGame: Game
  newRound: Round
}
