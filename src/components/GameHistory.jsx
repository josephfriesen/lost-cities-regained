import React from 'react';

export default function GameHistory(props) {
  return (
    <div>List of completed games goes here: Link for each completed game, which redirects to a Completed Game component showing the result of that game.</div>
  );
}
