import React from 'react';
import PropTypes from 'prop-types';
import { Switch, Route, withRouter } from 'react-router-dom';

class App extends React.Component {
  render() {
    return (
      <div>
        <pre>Place application ====> here</pre>
      </div>
    )
  }
}

App.propTypes = {
  
}

export default withRouter(App);
